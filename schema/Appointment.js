class Appointment {
  constructor(name, email, date, notes, status) {
    this.name = name
    this.email = email
    this.date = date
    this.notes = notes
    this.status = status
  }
}

module.exports = Appointment
