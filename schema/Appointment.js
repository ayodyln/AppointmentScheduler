class Appointment {
  constructor(name, email, date, time, notes, status) {
    this.name = name
    this.email = email
    this.date = date
    this.time = time
    this.notes = notes
    this.status = status
  }
}

module.exports = Appointment
